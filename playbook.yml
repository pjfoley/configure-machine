site:
  title: Machine setup
  url: https://docs.ifoley.id.au
  start_page: machinesetup::install.adoc
content:
  sources:
  - url: .
    branches: HEAD
ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/master/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

output:
  clean: true
  dir: ./public
  destinations:
  - provider: fs

